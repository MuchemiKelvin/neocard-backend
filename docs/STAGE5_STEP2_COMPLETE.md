# Stage 5 — Step 2 Complete: Device Identity Restored

**Date:** 2026-08-09  
**Status:** Step 2 closed (identity + credential). Step 3 not started.  
**Policy:** No API key values in this document. Git history not rewritten.

---

## Final report

| Item | Result |
|------|--------|
| Restored device identity | **YES** |
| Device ID | `device_1783962666378_8d478192` |
| Device name | `KDVC-RPI-001` |
| Device type | `fingerprint_device` |
| New credential provisioned | **YES** |
| `/v1/device/me` | **PASS** |
| Old credential revoked | **YES** (`401 INVALID_DEVICE_API_KEY`) |
| Database integrity | **PASS** (`ok`) |
| Backend tests | **45/45 passed** |
| Pi tests | **24/24 passed** |
| Admin API secret exposure fixed | **YES** (`api_key` not returned; `has_api_key: true`) |
| Live DB unchanged by tests | **YES** |
| Backend restart survival | **PASS** (same device_id/name after restart) |
| Pi app config reload survival | **PASS** (`.env` → settings reload matches new credential) |
| Pi reboot tested | **NO** (deferred to later E2E) |
| systemd | **NOT started** |

---

## What was done

1. Confirmed historical legitimacy via live `sync_logs` (Kelvin + other assignments to this `device_id`).  
2. Backed up live DB.  
3. Restored row via admin `POST /v1/hardware/devices` with explicit `device_id` (application path; fresh key generated by backend).  
4. Assigned Kelvin `user_1784053930781_e82767e8`.  
5. Updated `kdvc-fingerprint/.env` `DEVICE_API_KEY` to the new credential (not printed).  
6. Verified `/v1/device/me` returns correct id/name and does not include `api_key`.  
7. Verified old compromised credential returns `401`.  
8. Fixed admin GET to return `has_api_key` without plaintext `api_key`.  
9. Ran backend + Pi suites; integrity check OK.  
10. Removed temporary key files from `/tmp`.

`fp_test_device_*` remains in DB as a non-production leftover and was not used as KDVC identity.

---

## Remaining risks

1. **Physical Pi filesystem:** This session updated the laptop checkout `kdvc-fingerprint/.env`. If the Raspberry Pi has a separate on-device `.env`, copy/sync the new `DEVICE_API_KEY` there before physical E2E (do not commit `.env`).  
2. **Fingerprint enrollment:** KDVC enrollments were wiped earlier — re-enroll Kelvin’s slot on R503 before demo check-in.  
3. **Git history** still contains old key literals — acceptable after revoke; history rewrite deferred.  
4. **Temp create response** may exist in local process logs from this session’s tooling — avoid sharing `/tmp` or server logs that might have captured create JSON; rotate again if those logs were exposed externally.

---

## Next

**Step 3 — systemd auto-start** (no architecture changes).
